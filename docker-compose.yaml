services:
  postgres:
    image: postgres:17.9-alpine
    ports:
      - ${DATABASE_PORT}:5432
    volumes:
      - boilerplate-db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}

  maildev:
    build:
      context: .
      dockerfile: maildev.Dockerfile
    ports:
      - ${MAIL_CLIENT_PORT}:1080
      - ${MAIL_PORT}:1025

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

  # Uncomment to use redis
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - 6379:6379

  api:
    build:
      context: .
      dockerfile: Dockerfile
    # The selected environment file, injected into the container.
    # Defaults to env/.env.local when ENV_FILE is not set.
    env_file:
      - ${ENV_FILE:-./env/.env.local}
    ports:
      - ${APP_PORT}:${APP_PORT}
    volumes:
      # FILE_DRIVER=local writes here; without a volume every uploaded file is
      # lost when the container is rebuilt.
      - boilerplate-uploads:/usr/src/app/upload

volumes:
  boilerplate-db:
  boilerplate-uploads:
